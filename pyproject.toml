[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seedance-api"
version = "0.1.0"
description = "Python wrapper for the CyberBara full-access Seedance API family"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
  { name = "Zero Lu", email = "zerolu.eth@gmail.com" }
]
dependencies = []
keywords = ["seedance", "seedance api", "cyberbara", "video generation", "sdk"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://github.com/ZeroLu/seedance-API"
Repository = "https://github.com/ZeroLu/seedance-API"
Issues = "https://github.com/ZeroLu/seedance-API/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
